{
	"scripts": {
		"dev": "concurrently \"pnpm run backend\" \"pnpm run user-interface\"",
		"backend": "cd backend && pnpm run dev",
		"user-interface": "cd user-interface && pnpm run dev",
		"extract-cbc": "node backend/models/extractCBC.js",
		"seed:curriculum": "node backend/seeders/curriculumSeeder.js"
	},
	"devDependencies": {
		"concurrently": "^9.2.0"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.71.2",
		"mongoose": "^8.17.2",
		"pdf-parse": "^1.1.1",
		"react-markdown": "^10.1.0"
	}
}
